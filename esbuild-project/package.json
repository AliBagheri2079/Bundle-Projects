{
  "name": "esbuild-project",
  "version": "1.0.0",
  "private": true,
  "description": "",
  "scripts": {
    "build": "esbuild assets/js/index.js --bundle --outfile=dist/main.js --minify --sourcemap --target=chrome58,firefox57,safari11,edge16",
    "watch": "npm run build -- --watch",
    "analyze": "npm run watch -- --analyze"
  },
  "keywords": [],
  "author": "ali bagheri",
  "license": "ISC",
  "dependencies": {
    "esbuild": "^0.14.42",
    "esbuild-plugin-sass": "^1.0.1"
  }
}
